# Introduction

To construct a pipeline to use and evaluate the cuVGL package. This include two parts:
1. Map construction with the [Isaac Mapping ROS package](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_mapping_and_localization/isaac_mapping_ros/index.html). We currently use a end-to-end map creation script provided by this package, but the relevant usages cannot be found in the 3.2 docs so we have to look for in in the 4.X docs.
2. Global localization using the cuVGL package. This package depends on the map generated by the. We have wrote a ROS2 launch file to start it together with cuVSLAM.

This is a workspace based on the [Isaac ROS official Doc](https://nvidia-isaac-ros.github.io/v/release-4.1/getting_started/index.html#create-a-workspace). All the Isaac Ros functionalities are run in a [containerized environment](https://nvidia-isaac-ros.github.io/v/release-3.2/concepts/docker_devenv/index.html#development-environment).

# System Requirement

- Hardware: Jetson Orin
- Ros2 version: Humble
- Isaac Ros version: 3.2

# Project Structure

```
.
├── data  # rosbags
├── docs  # supplymentary text files
├── download_NGC_assets_for_ess.sh  # script
├── download_NGC_assets_for_nvblox.sh  # script
├── output  # artifact
├── justfile  # common commands used in the local machine
├── makefile  # common commands used in the container
└── src
    ├── isaac_localization_launch  # cuVGL launch package
    ├── isaac_ros_common  # docker files
    └── mapping  # maps creation
```

# Quick Start

## On local machine

Set Up Development Environment

```bash
just set_up_development_environment
```

Enter the dev container

```bash
just docker
```

## Map Creation

Create the maps (vslam map and vgl map)

```bash
cd src/mapping
make create_map_offline
```

## Test cuVGL

Build/install launch files

```bash
cd src/isaac_localization_launch
make build
source install/setup.bash
```

Remove build artifacts

```bash
make clean
```

Run cuVGL

```bash
make run
# then open another terminal
make replay
```

# Data

Recorded at MingDa 5F on 2026/03/04.
To download: `just download-data`

## Contents

- 2 rosbags for map creation
    - name ended with build or build_map
- 7 rosbags for testing
    - left/bottom stand for the starting line
    - the numbers show the offset in cm along the starting line from the bottom-left corner 
